apiVersion: v1
kind: Service
metadata:
  name: jwebapp-backend-service
  labels:
    app: jwebapp-backend
spec:
  selector:
    app: jwebapp-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
